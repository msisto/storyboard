# Copy to .env and fill in to run `npm run chromatic` locally.
# Not required for CI — the GitHub Actions workflow uses the
# CHROMATIC_PROJECT_TOKEN repo secret instead.
# Get your token at https://www.chromatic.com/ under Manage → Configure.
CHROMATIC_PROJECT_TOKEN=

{
  "name": "storyboard",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run storybook -w packages/storybook\" \"npm run dev -w packages/app\" \"npm run dev -w packages/server\"",
    "build": "npm run build -w packages/app"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
